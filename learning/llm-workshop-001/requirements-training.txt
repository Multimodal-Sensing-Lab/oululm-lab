# Torch is intentionally separate: desktop CUDA wheel vs CSC's ARM/CUDA module.
# These library versions are the CPU-tested first implementation stack.
transformers==5.16.1
peft==0.20.0
accelerate==1.14.0
datasets==5.0.1
pyyaml==6.0.3
scikit-learn>=1.3,<2
# Progressive training plots in the classroom and CSV domain demos.
matplotlib>=3.7,<4
