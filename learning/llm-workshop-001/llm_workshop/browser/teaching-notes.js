/* Shared presentation notes. Counts come from the loaded checkpoint, not the prompt. */
(function(root){
  'use strict';
  const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const number=n=>n.toLocaleString('en-US');
  const count=x=>Array.isArray(x)?x.reduce((sum,value)=>sum+count(value),0):1;
  function parameters(model){
    const groups={};
    for(const [key,value] of Object.entries(model.weights)){
      const name=key.startsWith('blocks.')?key.split('.').slice(0,2).join('.'):key.split('.')[0];
      groups[name]=(groups[name]||0)+count(value);
    }
    const labels={token_embedding:'Token embedding table',position_embedding:'Position embedding table',ln_f:'Final normalization',lm_head:'Output weights + biases'};
    const total=Object.values(groups).reduce((a,b)=>a+b,0);
    return `<details class="curiosity"><summary>Where do the ${number(total)} parameters come from?</summary><p>A parameter is one stored, adjustable number. Reusing it at several input positions does not create new parameters.</p><table class="note-table"><thead><tr><th>Component</th><th>Stored numbers</th></tr></thead><tbody>${Object.entries(groups).map(([key,value])=>`<tr><td>${labels[key]||`Transformer block ${Number(key.split('.')[1])+1}`}</td><td>${number(value)}</td></tr>`).join('')}<tr><th>Total</th><th>${number(total)}</th></tr></tbody></table><p>Block counts include attention, feed-forward and normalization parameters. Training changes values; editing a prompt only changes the input and calculated results.</p><p><strong>Size reference:</strong> these parameters alone would occupy about ${number(Math.round(total*4/1024))} KiB at 4 bytes each (FP32). This is not the browser download size or total runtime memory.</p></details>`;
  }
  function dimensions(cfg,ids,vocabulary){
    const n=ids.length,d=cfg.n_embd,v=cfg.vocab_size,c=cfg.block_size;
    const preview=ids.slice(0,8).join(', ')+(n>8?', …':'');
    return `<section class="dimension-guide" aria-label="Stored embedding tables and current lookup results"><h3>Fixed tables. One selected row per input token.</h3><div class="dimension-cards"><div class="dimension-card"><span>STORED · WHICH TOKEN?</span><strong>Wtoken: ${v} × ${d}</strong><small>${v} vocabulary entries × ${d} features</small></div><div class="dimension-card"><span>STORED · WHICH POSITION?</span><strong>Wposition: ${c} × ${d}</strong><small>Positions 0–${c-1} × ${d} features</small></div><div class="dimension-card selected"><span>SELECTED FOR THIS INPUT</span><strong>${n} × ${d} per lookup</strong><small>${n} token positions × ${d} features</small></div></div><p class="dimension-caption">The matrices below are <strong>lookup results</strong>, not the full stored tables. Their row count follows your input; the tables do not resize.</p><details class="curiosity compact"><summary>Why do the dimensions change with my sentence?</summary><p>Token IDs [${preview}] select rows from Wtoken. Input positions 0–${n-1} select rows from Wposition. A repeated token selects the same token row again, but uses a different position row.</p><p>First input token: <strong>${esc(vocabulary[ids[0]])}</strong> → Wtoken[${ids[0]}] + Wposition[0]. Each vector has ${d} features, so their sum still has ${d} features.</p><p><strong>Vocabulary size is not sentence length.</strong> Tokens can repeat. The ${c} in Wposition counts supported positions; ${d} counts features. These are independent choices. A hypothetical 128-position model with ${d} features would store Wposition with shape 128 × ${d}.</p><p>Shapes are fixed for this configured model. Our character and word vocabularies were built from training text; tokenizer preparation is separate from training the transformer. Other architectures can encode position without a learned table like this one.</p></details>${tokenization(cfg)}${context(cfg)}</section>`;
  }
  function scale(){
    return `<details class="curiosity purple"><summary>Curiosity · how large can a token table be?</summary><p>Token-table parameters = vocabulary entries × model width (V × d). These are configuration examples, not a capability ranking.</p><div class="note-scroll"><table class="note-table"><thead><tr><th>Model</th><th>V</th><th>d</th><th>V × d</th></tr></thead><tbody><tr><td>Our characters</td><td>32</td><td>64</td><td>2,048</td></tr><tr><td>Our words</td><td>52</td><td>64</td><td>3,328</td></tr><tr><td>GPT-2 small</td><td>50,257</td><td>768</td><td>38,597,376</td></tr><tr><td>Gemma 2B · report v4</td><td>256,128</td><td>2,048</td><td>524,550,144</td></tr></tbody></table></div><p>A larger vocabulary can shorten token sequences but increases the table size; token counts also depend on the text and tokenizer. Wider vectors increase capacity and computation, without assigning a human meaning to each coordinate. Block inputs/outputs keep width d; internal layers can be wider (our feed-forward layer uses 64 → 256 → 64).</p><p>Gemma's table alone is about 1.05 GB at 2 bytes per parameter, excluding runtime memory. Gemma shares input/output embeddings and uses rotary positions. Our model uses separate input/output weights and a learned position table.</p><p class="note-source">Sources: <a href="https://huggingface.co/openai-community/gpt2/blob/main/config.json">GPT-2 configuration</a> · <a href="https://arxiv.org/html/2403.08295v4">Gemma report, tables 1–2</a>. Named versions matter; rounded vocabulary sizes may omit reserved entries.</p></details>${tokenTradeoff()}`;
  }
  function tokenization(cfg){
    return `<details class="curiosity compact"><summary>Curiosity · tokenization is not embedding</summary><p><strong>Two steps:</strong> text → integer IDs → learned vectors. In our character model, <code>a → 9 → row 9 of Wtoken</code>. ID 9 is an address, not a meaning or a score. IDs depend on the tokenizer; the word companion has different IDs.</p><table class="note-table"><thead><tr><th>Step</th><th>Operation</th><th>Changed by transformer training?</th></tr></thead><tbody><tr><td>Tokenization</td><td>Text → integer IDs</td><td>No. The tokenizer’s rules and vocabulary stay fixed in this run.</td></tr><tr><td>Embedding</td><td>ID → a row of ${cfg.n_embd} numbers</td><td>Yes, when the embedding parameters are trainable; frozen embeddings stay fixed.</td></tr></tbody></table><p>A tokenizer can first be fitted to a corpus—for example, learning BPE merge rules. That preparation is separate from backpropagation through this transformer. During this workshop’s model training, gradients update neural weights, not the text-to-ID mapping. During inference, both the tokenizer and embedding weights stay fixed.</p><p><strong>Fixed vocabulary does not mean fixed sentences.</strong> Existing characters or subwords can be combined into text the model has never seen. A new word in a prompt does not automatically add a vocabulary entry. Our whole-word companion is deliberately more restrictive: it rejects words outside its small list.</p><p><strong>Fixed does not mean forever:</strong> a tokenizer can be deliberately extended or replaced, but its IDs must remain aligned with the model’s embedding and output rows. Embeddings are not guaranteed to change on every step; freezing and the training setup determine which parameters receive updates.</p><p class="note-source">Source: <a href="https://huggingface.co/docs/transformers/main/tokenizer_summary">Tokenizer preparation and algorithms</a>. These statements describe our conventional tokenizer-plus-transformer setup.</p></details>`;
  }
  function context(cfg){
    const c=cfg.block_size,d=cfg.n_embd;
    const words=c===16;
    return `<details class="curiosity compact"><summary>Curiosity · what happens beyond ${c} positions?</summary><p><strong>${c} positions = at most ${c} tokens in one forward pass.</strong> This is this model’s <strong>context window</strong>. Instructions, background, your question and any other supplied text share these same slots; they do not each get a separate window. Generated tokens also use positions as the sequence grows. The learned position table has rows 0–${c-1}. Token ${c} uses row ${c-1}; a further token would need the missing row ${c}.</p><p>${words?'The word companion counts START as one position, leaving at most 15 word/punctuation tokens in an input.':'The character route counts spaces and punctuation as tokens too.'} This browser <strong>rejects an overlong input</strong> and stops generation when full. It does not silently truncate your text. The length check runs before a lookup could go out of bounds.</p><p><strong>What about 3,000 words?</strong> That is far beyond this tiny window. The exact token count depends on the text and tokenizer; there is no universal word-to-token conversion. Count tokens with the intended tokenizer and leave room for the generated answer.</p><p><strong>Why not just enlarge the table?</strong> A hypothetical million-position learned table at width ${d} would contain ${number(1000000*d)} position parameters. At width 2,048, it would contain 2.048 billion: about 4.10 GB at two bytes per value, before other weights or runtime memory.</p><p><strong>RoPE:</strong> many models rotate query and key coordinates according to position instead of learning one vector per position. Implementations can cache rotations, but these are not millions of learned position rows. RoPE alone does not guarantee reliable unlimited context: training, attention computation and runtime memory still matter. <a href="https://arxiv.org/abs/2104.09864">RoFormer paper</a>.</p><p><strong>Dated scale references:</strong> original Gemma (2024): 8,192 tokens; Llama 3.1 (2024): 128K; Gemini 1.5 Pro’s May 2024 release: one million. These are particular releases, not promises for every model or application. A long window also does not guarantee that every detail will be used correctly.</p><p><strong>If a document is too long:</strong> a system may reject it, truncate it, process overlapping chunks, summarize it, or retrieve selected passages (RAG). Sliding windows can drop earlier context; RAG supplies selected evidence and can miss relevant passages. Retrieval may use keywords, vectors or both—it does not require a vector database.</p><p>Prompt processing can handle many positions in parallel or in chunks; generating a continuation still proceeds token by token. Our browser implements neither long-context processing nor automatic retrieval for these inputs.</p><p class="note-source">Sources: local engine length checks · <a href="https://huggingface.co/google/gemma-2b">Gemma model card</a> · <a href="https://github.com/meta-llama/llama-models/blob/main/models/llama3_1/MODEL_CARD.md">Llama 3.1 model card</a> · <a href="https://blog.google/innovation-and-ai/products/google-gemini-update-flash-ai-assistant-io-2024/">Gemini, May 2024</a> · <a href="https://arxiv.org/abs/2005.11401">RAG paper</a>.</p></details>`;
  }
  function billing(){
    return `<details class="curiosity purple"><summary>Curiosity · tokens also show up on the bill</summary><p>Many hosted APIs charge for <strong>input tokens</strong> (what you send) and <strong>output tokens</strong> (what the model generates), often at different rates. At the same model, rates and billing conditions, more billed tokens means more cost.</p><p><strong>Simplified cost:</strong> input tokens × input rate + output tokens × output rate. If rates are quoted per million tokens, divide each token count by one million first. Cache pricing, batch discounts, context tiers and tool fees can change the total.</p><p><strong>Made-up example, not a current price:</strong> at €1 per million input tokens and €4 per million output tokens, 1,000 input + 500 output tokens cost €0.003. Repeating that request 1,000 times costs €3, before any other fees.</p><p>You pay for token occurrences processed or generated, not for the number of entries in the vocabulary. Resent conversation history can count as input again. A chat subscription has its own terms; this local browser demo makes no paid API calls.</p><p class="note-source">Example of a provider’s billing categories: <a href="https://www.anthropic.com/pricing">Anthropic pricing</a>. Check the chosen provider and model’s current rates.</p></details>`;
  }
  function tokenTradeoff(){
    return `<details class="curiosity purple"><summary>Curiosity · fewer tokens, but a larger lookup table?</summary><p><strong>Illustration:</strong> <code>understand</code> has ten characters: <code>u n d e r s t a n d</code>. A subword tokenizer might encode it as one token or several. One token is possible only if that tokenizer’s vocabulary and splitting rules allow it in that context; we have not measured this example with Gemma. Our tiny word model only accepts its own fixed vocabulary.</p><p><strong>Shorter sequence:</strong> fewer tokens means fewer positions to process. For ordinary dense attention, the score grid grows as T × T: 100 positions give 10,000 cells per head; 50 give 2,500. This comparison is not a claim that the whole model runs four times faster.</p><p><strong>Larger table:</strong> each vocabulary entry adds d stored numbers. At width 64, adding 1,000 entries adds 64,000 parameters. Our character table holds 32 × 64 = 2,048 values. Gemma 2B’s specified report table holds 256,128 × 2,048 = 524,550,144 values: 1,049,100,288 bytes, approximately <strong>1.05 GB (0.98 GiB)</strong>, at two bytes each. This counts that table only, not the whole model or runtime memory.</p><p><strong>Text matters:</strong> tokenizer training data, language, spelling, spaces, punctuation and code all affect how text splits. Finnish, Spanish, English and equations can have different token counts; none is guaranteed to split in a particular way just from its language. Even equal-sized vocabularies can produce different counts. Measure the actual text with the actual tokenizer.</p><p class="note-source">Sources: <a href="https://huggingface.co/docs/transformers/main/tokenizer_summary">Tokenizer algorithms</a> · <a href="https://arxiv.org/html/2403.08295v4">Gemma report v4</a>. The attention-grid counts and byte sizes are arithmetic examples.</p></details>${billing()}`;
  }
  function reasoning(){
    return `<details class="curiosity purple"><summary>Curiosity · how can token prediction support reasoning?</summary><p><strong>More steps:</strong> generated intermediate text becomes context for later predictions, like a scratchpad. It can help on some tasks, but a plausible derivation can still be wrong. <a href="https://arxiv.org/abs/2201.11903">Chain-of-thought study</a>.</p><p><strong>Training feedback:</strong> DeepSeek-R1-Zero used rule-based answer and format rewards. A reward for the final answer does not certify every intermediate step. <a href="https://arxiv.org/html/2501.12948v1">DeepSeek-R1 report</a>.</p><p><strong>Search and checking:</strong> some systems try alternative paths or call external tools. Lean checks formal proofs against a formal statement and its assumptions; the statement must still match the intended problem. <a href="https://lean-lang.org/doc/reference/latest/Introduction/">Lean reference</a>.</p><p class="note-caution"><strong>Historical example · IMO 2024:</strong> AlphaProof + AlphaGeometry 2 reached 28/42, a silver-medal-level score. Problems were formalized by people; some solutions took up to three days. This was a specialized combined system, not ordinary chat generation. <a href="https://deepmind.google/blog/ai-solves-imo-problems-at-silver-medal-level/">DeepMind report</a>.</p><p>These are different techniques, not a recipe used by every reasoning model. Our tiny browser model demonstrates the prediction loop; it does not implement reasoning training, search, tools or formal verification.</p></details>`;
  }
  function block(cfg,blockIndex=0,stage=null){
    const d=cfg.n_embd,heads=cfg.n_head,dh=d/heads;
    const steps=['1 · Normalize','2 · Q / K / V','3 · Scores','4 · Mask','5 · Softmax','6 · Mix values','7 · Residual + FFN'];
    const box=(y,h,label,active)=>`<rect x="85" y="${y}" width="250" height="${h}" rx="8" fill="${active?'#e6f4ee':'#f3f6fa'}" stroke="${active?'#087e70':'#c9d6e3'}" stroke-width="${active?3:1}"/><text x="210" y="${y+h/2+5}" text-anchor="middle" font-size="14" fill="#193c42">${label}</text>`;
    return `<details class="curiosity block-map"><summary>Where am I inside a transformer block?</summary><p><strong>Our actual TinyGPT · selected block ${blockIndex}; ${cfg.n_layer} blocks total.</strong> ${stage===null?'Follow the arrows from top to bottom.':esc(steps[stage])+' is selected below.'} Both blocks have this structure and their own learned weights.</p><div class="block-map-layout"><svg viewBox="0 0 430 510" role="img" aria-label="Our pre-normalization transformer block. Input passes through LayerNorm, causal self-attention, and a residual addition; then through LayerNorm, feed-forward, and another residual addition. Each addition also receives its sublayer input via a bypass."><defs><marker id="block-map-arrow" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto"><path d="M0 0 L6 3 L0 6Z" fill="#365d91"/></marker></defs><g fill="none" stroke="#365d91" stroke-width="2" marker-end="url(#block-map-arrow)"><path d="M210 29V55"/><path d="M210 95V120"/><path d="M210 180V212"/><path d="M210 248V275"/><path d="M210 315V340"/><path d="M210 395V427"/><path d="M210 463V491"/><path d="M210 39H35V230H85"/><path d="M210 260H395V445H335"/></g><text x="210" y="21" text-anchor="middle" font-size="14">Input X · T × ${d}</text>${box(55,40,'1 · LayerNorm',stage===0)}${box(120,60,'2–6 · Causal self-attention',stage!==null&&stage>=1&&stage<=5)}${box(212,36,'+ add original X',stage===6)}${box(275,40,'LayerNorm',stage===6)}${box(340,55,'Feed-forward: '+d+' → '+4*d+' → '+d,stage===6)}${box(427,36,'+ add sublayer input',stage===6)}<text x="210" y="507" text-anchor="middle" font-size="14">Output · T × ${d}</text><text x="22" y="157" transform="rotate(-90 22 157)" text-anchor="middle" font-size="12" fill="#365d91">residual bypass</text><text x="415" y="355" transform="rotate(90 415 355)" text-anchor="middle" font-size="12" fill="#365d91">residual bypass</text></svg><div><p><strong>Inside attention:</strong> project Q/K/V → compare Q with K → scale by √${dh} → mask future positions → softmax → mix V. Combine ${heads} heads of ${dh} features and project back to ${d}.</p><p><strong>Two different jobs:</strong> attention mixes information across allowed positions. The feed-forward network transforms each row separately, using the same weights at every position. GELU sits between its two linear layers.</p><p><strong>The bypass arrows:</strong> preserve the incoming representation and add an update. The + signs mean addition, not concatenation. Browser step 7 shows both additions and the feed-forward part.</p><p><strong>Before / after this block:</strong> embeddings feed block 0; block 0 feeds block 1. After the last block, final normalization and the vocabulary projection produce next-token scores.</p></div></div><p class="note-caution"><strong>Reading other diagrams:</strong> the original 2017 Transformer has encoder and decoder stacks, with normalization after residual additions. Our GPT-style model has causal self-attention, no encoder–decoder attention, and normalization before each sublayer (pre-LayerNorm). The diagram above follows our code; dropout is inactive in browser inference.</p><p class="note-source">Further reading: <a href="https://epichka.com/blog/2023/qkv-transformer/">Pichka’s Q/K/V intuition</a> · <a href="https://arxiv.org/html/1706.03762v7">original Transformer paper</a> · <a href="https://arxiv.org/abs/2002.04745">normalization placement</a>. Drawing based on <code>tiny.py: Block.forward</code> and <code>browser/engine.js</code>.</p></details>`;
  }
  function vocabulary(kind){
    return `<details class="curiosity compact"><summary>Vocabulary: who chooses the pieces and the size?</summary><p><strong>Vocabulary = the complete list of token pieces the model can read or predict, each with a fixed integer ID.</strong> The designer chooses the splitting rules and training text; the fitting algorithm builds the list.</p><ol><li>Choose training data and reserve validation/test data.</li><li>Choose units: characters, whole words, or subwords.</li><li>Fit the tokenizer on training text and add special markers.</li><li>Assign IDs; set the model's embedding/output row count to that vocabulary size.</li><li>Freeze this mapping while training the neural weights.</li></ol><p><strong>Here:</strong> the original character vocabulary has 31 distinct training characters + UNK = 32. The original word vocabulary has 49 observed word/punctuation pieces + BOS, EOS, UNK = 52. Those sizes are the result of these small corpora and rules; they were not selected because words require 52 entries.</p><p><strong>Subword tokenizers:</strong> a designer usually chooses a target vocabulary size; fitting selects frequent reusable pieces within that budget. Larger vocabularies can shorten sequences while increasing embedding/output tables. The tokenizer must remain aligned with its model. A new prompt does not add entries.</p><p><strong>Question marks:</strong> the first character stories do not contain “?”, so that checkpoint has no row for it. Removing the mark would not teach question answering. <a href="domain-lab.html?domain=qa">Use the separate question exercise</a>, whose vocabulary includes question syntax and whose training examples pair questions with answers.</p><p>The new CSV runs reserve one shared vocabulary from all planned TRAIN splits before pretraining, including the labelled authored Q&A set. Domain words receive IDs in advance, but only the chosen base corpus updates the base weights. Validation text never defines IDs.</p></details>`;
  }
  root.WorkshopNotes={vocabulary,parameters,dimensions,scale,reasoning,context,tokenTradeoff,block};
})(globalThis);
